<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Coupon</title>
    <!-- Include your CSS stylesheets or link to a styling framework like Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbs5Fad2TtmzY9y9G2NwviTqK4bD/i6zD4P5q2J60JZ4gMzjSH6mGXFYYPs5FS+D" crossorigin="anonymous">
    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            background-color: #f8f9fa;
        }

        .card {
            width: 400px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .card-body {
            padding: 20px;
        }

        .card-title {
            text-align: center;
            margin-bottom: 20px;
            font-size: 24px;
            color: #343a40;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            font-size: 16px;
            color: #495057;
        }

        .form-control {
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ced4da;
            border-radius: 4px;
        }

        .btn-primary {
            background-color: #007bff;
            color: #ffffff;
            border: none;
            border-radius: 4px;
            padding: 10px 20px;
            font-size: 18px;
            cursor: pointer;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="card-body">
            <h2 class="card-title">Add Coupon</h2>
            <form action="/addcoupon" method="post">

                <!-- Coupon Code -->
                <div class="form-group">
                    <label for="couponCode" class="form-label">Coupon Code:</label>
                    <input type="text" id="couponCode" name="couponCode" class="form-control" required>
                    <p><%= message %></p>
                </div>

                <!-- Discount Amount -->
                <div class="form-group">
                    <label for="discountAmount" class="form-label">Discount Amount:</label>
                    <input type="text" id="discountAmount" name="discountAmount" class="form-control" required>
                </div>

                <!-- Expiration Date -->
                <div class="form-group">
                    <label for="expirationDate" class="form-label">Expiration Date:</label>
                    <input type="date" id="expirationDate" name="expirationDate" class="form-control" required>
                </div>

                <!-- Minimum Purchase -->
                <div class="form-group">
                    <label for="minimumPurchase" class="form-label">Minimum Purchase:</label>
                    <input type="text" id="minimumPurchase" name="minimumpurchase" class="form-control" required>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn btn-primary">Add Coupon</button>
            </form>
        </div>
    </div>

    <!-- Include your JavaScript libraries or scripts if needed -->

    <!-- Bootstrap JS (optional, only if you need JavaScript features) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-4sNGiLECXW/owpAyo/zJjzmWUpAiklb/8EgFZ0U5tYh6Pxp1QbPnjr8qUpEI1/e2" crossorigin="anonymous"></script>

    <!-- jQuery (optional, only if you need JavaScript features) -->
    <script src="https://code.jquery.com/jquery-3.6.4.min.js" integrity="sha384-UO2eTJ3f3czI5SaXo2jzCk5PpKuUsx9r2D+BD5sFVGLhDMF9MQjFVgqkyvNUYQaS" crossorigin="anonymous"></script>
</body>
</html>
